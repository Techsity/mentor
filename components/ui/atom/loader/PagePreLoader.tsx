import React from "react";

const PagePreLoader = () => {
	return (
		<div className="z-50 fixed h-full w-full bg-black top-0 left-0 overflow-hidden min-h-screen min-w-screen">
			PagePreLoader
		</div>
	);
};

export default PagePreLoader;
